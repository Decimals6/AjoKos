<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    android:layout_marginHorizontal="16dp"
    card_view:cardCornerRadius="16dp"
    card_view:cardElevation="4dp"
    card_view:cardBackgroundColor="#E5DDAE">

    <LinearLayout
        android:id="@+id/budgetObject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Baris 1: Nama Budget + Total Budget -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvBudgetName"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Makanan"
                android:textSize="16sp"
                android:textColor="#08454C"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvBudgetAmount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Rp 500.000"
                android:textSize="16sp"
                android:textColor="#08454C"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Baris 2: Bulan -->
        <TextView
            android:id="@+id/tvBudgetMonth"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Februari"
            android:textSize="14sp"
            android:textColor="#08454C"
            android:layout_marginTop="4dp" />

        <!-- Baris 3: Usage -->
        <TextView
            android:id="@+id/tvBudgetUsage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Spent: 300.000 | Left: 200.000"
            android:textSize="14sp"
            android:textColor="#08454C"
            android:layout_marginTop="2dp" />

        <!-- Baris 4: Status (opsional) -->
        <TextView
            android:id="@+id/tvBudgetStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Status: Aktif"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="2dp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
